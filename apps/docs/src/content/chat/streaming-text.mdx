---
title: StreamingText
description: Plain-text incremental renderer with a blinking cursor while tokens arrive.
category: chat
status: stable
---

import Preview from '../../components/docs/Preview.astro';
import { StreamingTextDemo } from '../../components/demos/demos';

## Preview

<Preview>
  <StreamingTextDemo client:load />
</Preview>

## Installation

```bash
pnpm add nyxis-ui
```

## Usage

```tsx
import { StreamingText } from 'nyxis-ui';

<StreamingText text={partial} streaming={isStreaming} />;
```

## Notes

- For markdown content use `<StreamingMarkdown>` — it handles fenced code, lists, tables, GFM.
- The cursor character defaults to `▍` and can be overridden with the `cursor` prop.
