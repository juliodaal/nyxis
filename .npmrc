auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
# provenance=true requires GitHub Actions OIDC — re-enable from the
# release workflow instead of from local publishes. See CI block
# (Phase plan, third bloque).
